<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.salesrecord">

    <application
        android:allowBackup="true"
        android:icon="@drawable/sr_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/add_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".RegisterEmployee"
            android:label="@string/title_activity_register_employee"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".DistributerHome" />
        <activity android:name=".ManageCompany" />
        <activity android:name=".RegisterCompany" />
        <activity
            android:name=".RegisterCompnay"
            android:label="@string/title_activity_register_compnay"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login"
            android:parentActivityName=".MainActivity"
            android:theme="@style/SplashScreen.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.salesrecord.MainActivity" />
        </activity>
        <activity android:name=".SuperAdminHome" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/SplashScreen.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>